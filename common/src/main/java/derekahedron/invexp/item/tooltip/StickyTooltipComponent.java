package derekahedron.invexp.item.tooltip;

import net.minecraft.world.inventory.tooltip.TooltipComponent;

/**
 * Special tooltip that is still rendered when being hovered over with an item.
 */
public interface StickyTooltipComponent extends TooltipComponent {
}
